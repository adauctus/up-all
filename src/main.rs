use std::env;

fn help() {
    println!("usage: up-all [options]

Options:

  --cache        Force cache file use
  --no-cache     Disable cache file use
  --config=FILE  If specified, use the given file as the up-all configuration

Fly, you fools!
")
}

fn main() {
    // Take a look at this: https://github.com/BurntSushi/xsv/blob/master/src/main.rs
    // parse args / flags

    // open cache & config

    // run package managers

    // store results to cache
}
